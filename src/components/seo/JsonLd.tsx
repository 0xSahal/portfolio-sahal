// Inlines a JSON-LD <script> tag for structured data. Accepts a single object
// or an array (rendered as multiple `<script>` tags so Google parses each
// independently).
export default function JsonLd({ data }: { data: object | object[] }) {
  const items = Array.isArray(data) ? data : [data];
  return (
    <>
      {items.map((item, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(item) }}
        />
      ))}
    </>
  );
}
